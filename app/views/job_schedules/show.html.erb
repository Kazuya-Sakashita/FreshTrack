<div class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-lg w-full bg-white p-8 rounded-lg shadow-md">
    <h1 class="text-2xl font-bold text-purple-700 text-center mb-6">ジョブスケジュールの詳細</h1>

    <table class="table-auto w-full border-collapse border border-gray-300">
      <tbody>
        <tr>
          <th class="bg-gray-100 px-4 py-2 text-left font-medium border-b border-gray-300">Cron</th>
          <td class="px-4 py-2 border-b border-gray-300"><%= @job_schedule.cron %></td>
        </tr>
        <tr>
          <th class="bg-gray-100 px-4 py-2 text-left font-medium border-b border-gray-300">作成日</th>
          <td class="px-4 py-2 border-b border-gray-300">
            <%= @job_schedule.created_at.strftime('%Y-%m-%d %H:%M') %>
          </td>
        </tr>
        <tr>
          <th class="bg-gray-100 px-4 py-2 text-left font-medium border-b border-gray-300">更新日</th>
          <td class="px-4 py-2 border-b border-gray-300">
            <%= @job_schedule.updated_at.strftime('%Y-%m-%d %H:%M') %>
          </td>
        </tr>
      </tbody>
    </table>

    <div class="flex justify-center mt-6">
      <%= link_to '編集', edit_job_schedule_path(@job_schedule),
        class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded focus:outline-none focus:ring-2 focus:ring-blue-400" %>
    </div>
  </div>
</div>
